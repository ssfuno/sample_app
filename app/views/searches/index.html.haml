.recipe-container
  = form_with(url: recipe_search_path, local: true, class: "search-form") do |form|
    = form.text_field :keyword, placeholder: "投稿を検索する", class: "search-input"
    = form.submit "検索", class: "search-btn"
  - @recipes.each do |recipe|
    .recipe-list-item
      .image-div
        = image_tag recipe.photo.url
      .text-div
        .recipe-title
          = link_to recipe.title, recipe_path(recipe.id)
        .recipe-user-name
          = "作成者：#{recipe.user.name}"
        .recipe-lower-div
          .created-at
            = "投稿日：#{l recipe.created_at}"
          .link-to-edit
            - if user_signed_in?
              - if recipe.user.id == current_user.id
                = link_to '編集する', edit_recipe_path(recipe.id)
  = link_to 'レシピ追加', new_recipe_path